import styled from 'styled-components'

const Wrapper = styled.div`
  text-align: center;
  display: grid;
  justify-content: center;
  align-items: center;
  min-height: calc(100vh - 20rem);
`

const FreakedOutPalm = styled.img`
  margin: 0 auto;
  max-width: 4rem;
`

export default { FreakedOutPalm, Wrapper }
